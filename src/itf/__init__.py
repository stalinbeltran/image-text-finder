"""image-text-finder — patch-based paragraph-corner detection.

The package is laid out by DOMAIN, not by technical layer, and the layout is the
design: see docs/organizacion.md. The dependency direction is a contract (⑦) and
has its own test, because the violations that motivated it were all imports that
merely "worked".
"""
